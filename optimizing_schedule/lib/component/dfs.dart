import 'package:optimizing_schedule/component/shedule.dart';

List resultSubject = [];

void dfs(Map subjectCheck, List wantToSubject, List canDoSubject) {
  for (int i = 0; i < wantToSubject.length; i++) {
    for (int j = 0; j < canDoSubject.length; j++) {}
  }
}
